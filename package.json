{
  "name": "pos-design-system",
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-native UI platform using Web Components. v0 experimental.",
  "scripts": {
    "build:tokens": "node tokens/build-tokens.js",
    "build:js": "node esbuild.config.js",
    "build": "npm run build:tokens && npm run build:js",
    "preview": "npm run build && node server.js",
    "test": "web-test-runner",
    "test:watch": "web-test-runner --watch"
  },
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@open-wc/testing-helpers": "^3.0.1",
    "@web/test-runner": "^0.20.2",
    "@web/test-runner-playwright": "^0.11.1",
    "esbuild": "^0.20.0"
  }
}
